{
  "name": "formulario-club",
  "version": "1.0.0",
  "description": "Questionário de bem-estar para jogadores, com painel administrativo.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "check": "node --check app.js && node --check routes/auth.js && node --check routes/jogador.js && node --check routes/admin.js",
    "test": "npm run check && node scripts/render-check.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "connect-pg-simple": "^10.0.0",
    "dotenv": "^17.2.2",
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "express-session": "^1.18.2",
    "helmet": "^8.1.0",
    "pg": "^8.16.3"
  }
}
